(function() {
    'use strict';

    angular
        .module('botsifyApp')
        .config(config)
        .run(run);

    function config(httpProvider, locationProvider) {}

    function run() {}
})();
